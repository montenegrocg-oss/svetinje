---
import { localeConfig, type Locale } from "../i18n/config";

interface Props {
  currentLocale?: Locale;
}

const { currentLocale = "sr" } = Astro.props;
const locales = Object.entries(localeConfig) as [Locale, (typeof localeConfig)[Locale]][];
---

<nav class="language-switcher" aria-label="Избор језика">
  <ul>
    {locales.map(([locale, config]) => (
      <li>
        {config.available ? (
          <a class="language-current" href="/" lang={config.htmlLang} aria-current={locale === currentLocale ? "page" : undefined}>
            {config.label}
          </a>
        ) : (
          <span
            class="language-unavailable"
            lang={config.htmlLang}
            aria-disabled="true"
            title="Превод још није доступан"
          >
            <span>{config.label}</span>
            <small>ускоро</small>
          </span>
        )}
      </li>
    ))}
  </ul>
</nav>
