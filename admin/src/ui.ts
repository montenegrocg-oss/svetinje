import type { AdminRepositorySnapshot, AdminPlace, EditablePlaceRecord, NarrativeSection } from "./repository-content.ts";
import type { AdminSession } from "./types.ts";
import type { AdminRoute, RouteRepositorySnapshot } from "./route-repository.ts";

const escapeHtml = (value: unknown) => String(value ?? "").replace(/[&<>"']/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[character] ?? character));
const styles = `:root{color-scheme:light;--navy:#0b2944;--gold:#b68a37;--paper:#f7f3e9;--line:#d9d2c3;--muted:#69747d;font-family:Inter,system-ui,sans-serif}*{box-sizing:border-box}body{margin:0;background:#eef0ed;color:#17212a}a{color:inherit}.shell{max-width:86rem;margin:auto;padding:1.25rem}.top{background:var(--navy);color:white}.top .shell,.toolbar,.save-bar{display:flex;align-items:center;justify-content:space-between;gap:1rem}.brand{font:700 1.45rem Georgia,serif;text-decoration:none}.session,.help{font-size:.83rem;color:var(--muted)}.top .session{color:white;opacity:.8}.nav{background:white;border-bottom:1px solid var(--line)}.nav .shell,.actions,.source-list{display:flex;gap:.65rem;flex-wrap:wrap}.nav a,.editor-nav a{padding:.7rem 1rem;border-radius:.65rem;text-decoration:none}.nav a:hover,.nav a:focus-visible{background:var(--paper)}main{padding-block:1.5rem 3rem}.eyebrow{color:var(--gold);font-weight:800;letter-spacing:.12em;text-transform:uppercase;font-size:.75rem}h1,h2{font-family:Georgia,serif;color:var(--navy)}.grid,.editor{display:grid;gap:1rem}.stats{grid-template-columns:repeat(4,minmax(0,1fr))}.card,.panel{background:white;border:1px solid var(--line);border-radius:1rem;padding:1.1rem;box-shadow:0 .4rem 1.3rem rgb(12 37 56/.06)}.stat strong{display:block;font:700 2rem Georgia,serif;color:var(--navy)}.chip,.badge{display:inline-flex;padding:.28rem .55rem;border-radius:999px;background:var(--paper);font-size:.78rem}.badge.preview-on{background:#e5f1e2;color:#315d2d}.badge.preview-off{background:#f1efea;color:#606970}.toolbar{margin:1rem 0}.button{display:inline-flex;align-items:center;justify-content:center;border:0;border-radius:.7rem;background:var(--navy);color:white;padding:.72rem 1rem;text-decoration:none;font-weight:700;cursor:pointer;min-height:44px}.button.secondary,.icon-button{background:white;color:var(--navy);border:1px solid var(--line)}.button.danger{background:#9b2525;color:white}.button.danger:hover{background:#7f1d1d}.button:disabled{cursor:not-allowed;opacity:.55}.button:focus-visible,input:focus-visible,select:focus-visible,textarea:focus-visible,a:focus-visible{outline:3px solid #d4a94c;outline-offset:2px}input,select,textarea{width:100%;padding:.72rem;border:1px solid #b9c0c5;border-radius:.65rem;background:white;font:inherit}textarea{min-height:7rem;resize:vertical}.table{width:100%;border-collapse:collapse}.table th,.table td{text-align:left;padding:.72rem;border-bottom:1px solid #e5e0d5}.table a{font-weight:700;color:var(--navy)}.form{max-width:44rem}.field{display:grid;gap:.35rem;margin-bottom:1rem}.error{color:#8c2020}.success{border-color:#89a784;background:#f5fbf3}.facts,.editor-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:1rem}.facts dt{font-size:.78rem;color:var(--muted)}.facts dd{margin:.2rem 0 0;font-weight:650}.wide{grid-column:1/-1}.preview-control{display:flex;align-items:center;justify-content:space-between;gap:1rem;margin:1rem 0}.preview-control h2,.preview-control p{margin:.2rem 0}.preview-control__status{display:flex;align-items:center;gap:.65rem;flex-wrap:wrap}.preview-dialog{width:min(34rem,calc(100vw - 2rem));max-width:34rem;border:1px solid var(--line);border-radius:1rem;padding:1.25rem;color:#17212a}.preview-dialog::backdrop{background:rgb(11 41 68/.48)}.preview-dialog .actions{justify-content:flex-end;margin-top:1rem}.danger-zone{margin-top:2rem;border-color:#d8a8a8}.danger-zone h2,.danger-zone h3{margin-block:.15rem .55rem}.danger-zone__copy{max-width:46rem}.delete-dialog__identity{padding:.75rem;border-radius:.7rem;background:var(--paper);overflow-wrap:anywhere}.editor-nav{position:sticky;top:0;z-index:3;display:flex;gap:.35rem;overflow-x:auto;padding:.55rem;background:#eef0ed;border-bottom:1px solid var(--line)}.editor-nav a{white-space:nowrap;background:white}.editor-section{scroll-margin-top:5rem}.readonly{background:#f4f3ef}.alternate-row,.section-editor{border:1px solid #e5e0d5;border-radius:.8rem;padding:1rem;margin:.7rem 0}.paragraph{display:grid;grid-template-columns:1fr auto;gap:.5rem;margin:.5rem 0}.icon-button{border-radius:.55rem;padding:.55rem;cursor:pointer;min-width:44px;min-height:44px}.map-picker{height:22rem;border:1px solid var(--line);border-radius:.8rem;overflow:hidden;background:#e8ece8}.map-picker-note{display:grid;place-items:center;height:100%;padding:1rem;text-align:center;color:var(--muted)}.photo-grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(min(100%,22rem),1fr));gap:1rem}.photo-local-list{display:grid;grid-template-columns:repeat(auto-fit,minmax(11rem,1fr));gap:1rem}.photo-card{display:grid;gap:.75rem;border:1px solid var(--line);border-radius:.85rem;padding:.8rem;overflow:hidden;background:var(--paper)}.photo-card img{width:100%;aspect-ratio:4/3;object-fit:cover;border-radius:.55rem;background:#dfe3df}.photo-card__meta{display:grid;gap:.55rem;overflow-wrap:anywhere}.photo-card__heading{display:flex;align-items:center;justify-content:space-between;gap:.65rem}.photo-card__technical{font-size:.78rem;color:var(--muted)}.photo-card__technical summary{cursor:pointer;min-height:44px;display:flex;align-items:center}.photo-card__technical code{display:block;overflow-wrap:anywhere;margin-top:.25rem}.photo-card__actions{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:.65rem}.photo-card__primary-action{width:100%}.photo-drop{display:grid;place-items:center;min-height:9rem;border:2px dashed #aeb8b7;border-radius:.85rem;padding:1.25rem;text-align:center;background:#fafaf7}.photo-drop.is-dragging{border-color:var(--gold);background:#fff8e8}.photo-input{position:absolute;inline-size:1px;block-size:1px;overflow:hidden;clip:rect(0 0 0 0)}.photo-actions{display:flex;flex-wrap:wrap;align-items:center;gap:.65rem}.photo-rights{margin:.85rem 0 0}.save-bar{position:sticky;bottom:0;z-index:4;background:white;border:1px solid var(--line);border-radius:1rem;padding:.8rem 1rem;box-shadow:0 -.4rem 1.4rem #0c253812}.validation-summary:empty{display:none}.is-dirty{color:#8b5e12}.is-clean{color:var(--muted)}@media(max-width:48rem){.stats,.facts,.editor-grid{grid-template-columns:1fr}.wide{grid-column:auto}.table-wrap{overflow-x:auto}.top .shell,.save-bar,.preview-control{align-items:flex-start;flex-direction:column}.nav .shell{overflow-x:auto}.shell{padding-inline:1rem}.map-picker{height:18rem}.photo-grid{grid-template-columns:1fr}.photo-local-list{grid-template-columns:repeat(2,minmax(0,1fr))}.photo-card__actions{grid-template-columns:repeat(2,minmax(0,1fr))}.danger-zone .button,.preview-dialog .actions .button{width:100%}}@media(max-width:25rem){.photo-local-list,.photo-card__actions{grid-template-columns:1fr}}`;

function layout(title: string, session: AdminSession, body: string, script = "", editorAssets: false | true | "route" = false): Response {
  const identity = session.email ?? session.subject;
  const assets = editorAssets ? '<link rel="stylesheet" href="/assets/editor.css">' : "";
  const client = editorAssets ? `<script type="module" src="/assets/${editorAssets === "route" ? "route-editor" : "editor"}.js"></script>` : "";
  return new Response(`<!doctype html><html lang="sr"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width"><meta name="robots" content="noindex,nofollow"><title>${escapeHtml(title)} — Светиње администрација</title><style>${styles}</style>${assets}</head><body><header class="top"><div class="shell"><a class="brand" href="/">Светиње · администрација</a><span class="session">Пријављен: ${escapeHtml(identity)}</span></div></header><nav class="nav" aria-label="Главна навигација"><div class="shell"><a href="/">Преглед</a><a href="/places">Објекти</a><a href="/routes">Руте</a><a href="/places/new">Нови објекат</a></div></nav><main class="shell">${body}</main>${script ? `<script>${script}</script>` : ""}${client}</body></html>`, { headers: { "content-type": "text/html; charset=utf-8", "cache-control": "no-store", "content-security-policy": "default-src 'self'; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'; connect-src 'self' https://api.maptiler.com https://*.maptiler.com; img-src 'self' data: blob: https://api.maptiler.com https://*.maptiler.com https://media.svetinje.me; worker-src 'self' blob:; base-uri 'none'; frame-ancestors 'none'; form-action 'self'", "permissions-policy": "camera=(), microphone=(), geolocation=()", "referrer-policy": "no-referrer", "x-content-type-options": "nosniff", "x-frame-options": "DENY" } });
}

export function dashboardPage(session: AdminSession, snapshot: AdminRepositorySnapshot): Response {
  const status = Object.entries(snapshot.stats.statuses).map(([name, count]) => `<span class="chip">${escapeHtml(name)} · ${count}</span>`).join("");
  return layout("Преглед", session, `<p class="eyebrow">Приватни уређивачки простор</p><h1>Преглед</h1><section class="grid stats" aria-label="Статистика"><article class="card stat"><strong>${snapshot.stats.total}</strong>свих објеката</article><article class="card stat"><strong>${snapshot.stats.preview}</strong>у радном приказу</article><article class="card stat"><strong>${snapshot.stats.withCoordinates}</strong>са координатама</article><article class="card stat"><strong>${snapshot.stats.withoutCoordinates}</strong>без координата</article></section><section class="panel"><h2>Уређивачки статуси</h2><div class="source-list">${status}</div><p class="help">Садржај: Git грана <code>${escapeHtml(snapshot.branch)}</code>, HEAD <code>${escapeHtml(snapshot.state.headSha)}</code>.</p></section>`);
}

export function placesPage(
  session: AdminSession,
  snapshot: AdminRepositorySnapshot,
  options: { deletedId?: string; mediaCleanupIncomplete?: boolean } = {},
): Response {
  const rows = snapshot.places.map((place) => `<tr data-place-row data-search="${escapeHtml(`${place.preferredName} ${place.id}`.toLowerCase())}"><td><a href="/places/${encodeURIComponent(place.id)}">${escapeHtml(place.preferredName)}</a><br><span class="help">${escapeHtml(place.id)}</span></td><td>${escapeHtml(place.placeType ?? "—")}</td><td>${escapeHtml(place.editorialStatus)}</td><td>${escapeHtml(place.browseAreaId ?? "—")}</td><td>${place.latitude !== undefined ? "Да" : "Не"}</td><td><span class="badge ${place.inPreview ? "preview-on" : "preview-off"}">${place.inPreview ? "У радном приказу" : "Није у радном приказу"}</span></td><td><a href="/places/${encodeURIComponent(place.id)}/edit">Уреди</a></td></tr>`).join("");
  const deletedId = options.deletedId && /^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(options.deletedId) ? options.deletedId : undefined;
  const notice = deletedId
    ? `<section class="panel success" role="status"><strong>Објекат је обрисан.</strong><p class="help">${escapeHtml(deletedId)}</p>${options.mediaCleanupIncomplete ? '<p class="error">Git запис је обрисан, али чишћење једног или више медијских објеката није завршено.</p>' : ""}</section>`
    : "";
  return layout("Објекти", session, `${notice}<div class="toolbar"><div><p class="eyebrow">Git садржај</p><h1>Објекти</h1></div><a class="button" href="/places/new">Нови објекат</a></div><label class="field">Претрага по називу или ID-ју<input type="search" data-place-search autocomplete="off"></label><div class="panel table-wrap"><table class="table"><thead><tr><th>Назив / ID</th><th>Врста</th><th>Статус</th><th>Област</th><th>Координате</th><th>Радни приказ</th><th>Акција</th></tr></thead><tbody>${rows}</tbody></table></div>`, `const q=document.querySelector('[data-place-search]');q?.addEventListener('input',()=>{const v=q.value.trim().toLocaleLowerCase('sr');document.querySelectorAll('[data-place-row]').forEach(r=>{r.hidden=!r.dataset.search.includes(v)})});`);
}

export function placePage(session: AdminSession, place: AdminPlace): Response {
  const coordinate = place.latitude !== undefined ? `${place.latitude}, ${place.longitude}` : "Нема";
  return layout(place.preferredName, session, `<div class="toolbar"><div><p class="eyebrow">Објекат · само читање</p><h1>${escapeHtml(place.preferredName)}</h1></div><a class="button" href="/places/${encodeURIComponent(place.id)}/edit">Уреди објекат</a></div><dl class="grid facts panel"><div><dt>Технички ID</dt><dd>${escapeHtml(place.id)}</dd></div><div><dt>Slug</dt><dd>${escapeHtml(place.slug ?? "—")}</dd></div><div><dt>Врста</dt><dd>${escapeHtml(place.placeType ?? "—")}</dd></div><div><dt>Статус</dt><dd>${escapeHtml(place.editorialStatus)}</dd></div><div><dt>Општина</dt><dd>${escapeHtml(place.municipality ?? "—")}</dd></div><div><dt>Насеље</dt><dd>${escapeHtml(place.settlement ?? "—")}</dd></div><div><dt>Координате</dt><dd>${escapeHtml(coordinate)}</dd></div><div><dt>Радни приказ</dt><dd>${place.inPreview ? "Да" : "Не"}</dd></div><div><dt>Медији</dt><dd>${place.mediaCount}</dd></div></dl>${place.summary ? `<section class="panel"><h2>Сажетак</h2><p>${escapeHtml(place.summary)}</p></section>` : ""}<p><a href="/places">← Назад на објекте</a></p>`);
}

export function newPlacePage(session: AdminSession, supportedPlaceTypes: string[], headSha: string): Response {
  const options = supportedPlaceTypes.map((type) => `<option value="${escapeHtml(type)}">${escapeHtml(type)}</option>`).join("");
  return layout("Нови објекат", session, `<p class="eyebrow">Research scaffold</p><h1>Нови објекат</h1><p class="help">Чување прави два research фајла у једном Git commit-у и не мијења editorial-preview.</p><form class="panel form" data-create-form><label class="field">Српски назив<input name="preferredName" required></label><label class="field">Технички ID<input name="id" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*"></label><label class="field">Slug<input name="slug" required pattern="[a-z0-9]+(?:-[a-z0-9]+)*"></label><label class="field">Врста<select name="placeType">${options}</select></label><input type="hidden" name="expectedHeadSha" value="${escapeHtml(headSha)}"><button class="button">Сачувај research запис</button><p data-form-status role="status"></p></form>`, `const f=document.querySelector('[data-create-form]'),s=document.querySelector('[data-form-status]');f.addEventListener('submit',async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(f));const r=await fetch('/api/places',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)}),d=await r.json();if(!r.ok){s.textContent=d.error?.message??'Грешка';return}s.innerHTML='Сачувано. Commit: <code>'+d.commitSha+'</code>. <a href="/places/'+encodeURIComponent(d.place.id)+'/edit">Настави уређивање</a>'});`);
}

const optionList = (values: readonly string[], current?: string) => values.map((value) => `<option value="${escapeHtml(value)}"${value === current ? " selected" : ""}>${escapeHtml(value)}</option>`).join("");
const paragraphHtml = (paragraph: string) => `<div class="paragraph"><textarea data-paragraph>${escapeHtml(paragraph)}</textarea><button class="icon-button" type="button" data-remove-paragraph aria-label="Уклони пасус">×</button></div>`;
const sectionHtml = (section: NarrativeSection) => `<article class="section-editor" data-section data-section-id="${escapeHtml(section.id)}"><div class="toolbar"><h3>${escapeHtml(section.id)}</h3><div class="actions"><button class="icon-button" type="button" data-move-section="up" aria-label="Помјери одјељак навише">↑</button><button class="icon-button" type="button" data-move-section="down" aria-label="Помјери одјељак наниже">↓</button></div></div><label class="field">Наслов<input data-section-title value="${escapeHtml(section.title)}"></label><div data-paragraphs>${section.paragraphs.map(paragraphHtml).join("")}</div><button class="button secondary" type="button" data-add-paragraph>Додај пасус</button></article>`;

export function editPlacePage(session: AdminSession, record: EditablePlaceRecord, mapTilerKey?: string): Response {
  const p = record.place;
  const alt = p.alternateNames.map((a) => `<div class="alternate-row" data-alternate-row><div class="editor-grid"><label class="field">Назив<input data-alt-name value="${escapeHtml(a.name)}"></label><label class="field">Статус<select data-alt-status>${optionList(record.options.verificationStatuses, a.verificationStatus)}</select></label><label class="field wide">Контекст<textarea data-alt-context>${escapeHtml(a.context)}</textarea></label></div><button type="button" class="button secondary" data-remove-alternate>Уклони назив</button></div>`).join("");
  const areas = `<option value=""${p.browseAreaId ? "" : " selected"}>— Није додијељена —</option>${record.options.browseAreas.map((a) => `<option value="${a.id}"${a.id === p.browseAreaId ? " selected" : ""}>${escapeHtml(a.label)}</option>`).join("")}`;
  const available = record.options.narrativeSectionIds.filter((id) => !p.sections.some((s) => s.id === id));
  const hasMultipleMedia = p.media.length > 1;
  const media = p.media.map((item, index) => {
    const label = item.isPrimary ? "Главна фотографија" : `Фотографија ${index + 1}`;
    const heading = item.isPrimary
      ? `${hasMultipleMedia ? `<strong>${label}</strong>` : ""}<span class="badge" data-primary-photo>${hasMultipleMedia ? "Главна" : label}</span>`
      : `<strong>${escapeHtml(label)}</strong>`;
    const primaryAction = !item.isPrimary && hasMultipleMedia
      ? `<button class="button secondary photo-card__primary-action" type="button" data-set-primary-photo>Постави као главну</button>`
      : "";
    return `<article class="photo-card" data-existing-photo data-media-id="${escapeHtml(item.id)}"><img src="${escapeHtml(item.src)}" alt="${escapeHtml(item.altText)}" loading="lazy"><div class="photo-card__meta"><div class="photo-card__heading">${heading}</div><span class="help">${escapeHtml(item.width && item.height ? `${item.width} × ${item.height}` : item.mimeType)}</span>${primaryAction}<label class="field">Алтернативни опис<input type="text" value="${escapeHtml(item.altText)}" data-photo-alt></label><div class="photo-card__actions"><button class="button secondary" type="button" data-save-photo-alt>Сачувај опис</button><button class="button secondary" type="button" data-delete-photo>Уклони</button></div><details class="photo-card__technical"><summary>Технички подаци</summary><code>${escapeHtml(item.id)}</code><code>${escapeHtml(item.objectKey)}</code></details></div></article>`;
  }).join("");
  const previewControl = `<section class="panel preview-control" data-preview-control data-preview-enabled="${p.inPreview}"><div><h2>Радни приказ</h2><div class="preview-control__status"><span class="badge preview-on" data-preview-badge${p.inPreview ? "" : " hidden"}>У радном приказу</span><p data-preview-description>${p.inPreview ? "" : "Објекат још није видљив на радној верзији сајта."}</p></div><p class="help">Промјена ће бити видљива након завршетка радне изградње.</p><p data-preview-status role="status"></p></div><button class="button${p.inPreview ? " secondary" : ""}" type="button" data-preview-toggle>${p.inPreview ? "Уклони из радног приказа" : "Додај у радни приказ"}</button><dialog class="preview-dialog" data-preview-dialog><form method="dialog"><h2>Уклони из радног приказа</h2><p>Објекат ће нестати са радне верзије сајта, али његови подаци и фотографије остају сачувани у администрацији.</p><div class="actions"><button class="button secondary" value="cancel">Откажи</button><button class="button" value="confirm">Уклони из радног приказа</button></div></form></dialog></section>`;
  const dangerZone = `<section class="panel danger-zone" aria-labelledby="delete-place-heading"><p class="eyebrow">Опасна зона</p><h2 id="delete-place-heading">Обриши објекат</h2><p class="danger-zone__copy">Ова радња трајно уклања радни запис објекта и не може се поништити кроз администрацију.</p><button class="button danger" type="button" data-delete-place-open>Обриши објекат</button><dialog class="preview-dialog" data-delete-place-dialog><h2>Трајно обрисати објекат?</h2><p class="delete-dialog__identity"><strong>${escapeHtml(p.preferredName)}</strong><br><code>${escapeHtml(p.id)}</code></p><p>Биће уклоњени запис објекта, његов српски текст, радни приказ и медији који припадају искључиво овом објекту.</p><label class="field">За потврду унесите ID објекта:<input type="text" autocomplete="off" spellcheck="false" data-delete-place-confirmation></label><p class="error" data-delete-place-status role="alert"></p><div class="actions"><button class="button secondary" type="button" data-delete-place-cancel>Откажи</button><button class="button danger" type="button" data-delete-place-submit disabled>Трајно обриши</button></div></dialog></section>`;
  const body = `<div class="toolbar"><div><p class="eyebrow">Уређивање објекта</p><h1>${escapeHtml(p.preferredName)}</h1><p class="help">ID <code>${escapeHtml(p.id)}</code> је непромјенљив · ${escapeHtml(p.editorialStatus)}</p></div><a class="button secondary" href="/places/${encodeURIComponent(p.id)}">Само читање</a></div>${previewControl}<nav class="editor-nav"><a href="#osnovno">Основно</a><a href="#crkveni">Црквени подаци</a><a href="#lokacija">Локација</a><a href="#foto">Фото</a><a href="#tekst">Српски текст</a></nav><form class="editor" data-place-editor data-place-id="${escapeHtml(p.id)}" data-head-sha="${record.state.headSha}" data-map-key="${escapeHtml(mapTilerKey ?? "")}">
  <section class="panel editor-section" id="osnovno"><h2>Основно</h2><div class="editor-grid"><label class="field">Технички ID<input class="readonly" value="${escapeHtml(p.id)}" readonly></label><label class="field">Пожељни назив<input name="preferredName" value="${escapeHtml(p.preferredName)}" required></label><label class="field">Краћи назив<input name="shortName" value="${escapeHtml(p.shortName ?? "")}"></label><label class="field">Slug<input name="slug" value="${escapeHtml(p.slug ?? "")}" required></label><label class="field">Врста<select name="placeType">${optionList(record.options.placeTypes, p.placeType)}</select></label><label class="field">Област<select name="browseAreaId">${areas}</select></label><label class="field wide">Сажетак<textarea name="summary">${escapeHtml(p.summary ?? "")}</textarea></label></div><h3>Алтернативни називи</h3><div data-alternate-list>${alt}</div><button class="button secondary" type="button" data-add-alternate>Додај назив</button></section>
  <section class="panel editor-section" id="crkveni"><h2>Црквени подаци</h2><label class="field">Епархија / јурисдикција<input name="jurisdiction" value="${escapeHtml(p.jurisdiction ?? "")}"></label><p class="help">Промјена структуриране чињенице захтијева нову провјеру.</p></section>
  <section class="panel editor-section" id="lokacija"><h2>Локација</h2><div class="editor-grid"><label class="field">Код државе<input name="countryCode" maxlength="2" value="${escapeHtml(p.countryCode ?? "ME")}"></label><label class="field">Општина<input name="municipality" value="${escapeHtml(p.municipality ?? "")}"></label><label class="field">Насеље<input name="settlement" value="${escapeHtml(p.settlement ?? "")}"></label><label class="field">Поштанска адреса<input name="postalAddress" value="${escapeHtml(p.postalAddress ?? "")}"></label><label class="field">Ширина<input name="latitude" type="number" step="any" value="${escapeHtml(p.latitude ?? "")}"></label><label class="field">Дужина<input name="longitude" type="number" step="any" value="${escapeHtml(p.longitude ?? "")}"></label><label class="field">Тачност<select name="coordinateAccuracy">${optionList(record.options.coordinateAccuracy, p.coordinateAccuracy ?? "complex-centroid")}</select></label><label class="field">Јавна безбједност<select name="publicationSafety">${optionList(record.options.publicationSafety, p.publicationSafety ?? "review-required")}</select><span class="help">За објекат у радном приказу координате морају бити означене као јавне.</span></label></div><button class="button secondary" type="button" data-clear-point>Обриши тачку</button><div class="map-picker" data-coordinate-map><div class="map-picker-note">${mapTilerKey ? "Учитавање уређивачке карте…" : "Карта није доступна без PUBLIC_MAPTILER_KEY; могућ је ручни унос."}</div></div><p class="help">Кликните карту или превуците ознаку.</p></section>
  <section class="panel editor-section" id="foto"><h2>Додај фотографије</h2><div class="photo-grid" data-existing-photo-list>${media || "<p data-photo-empty>Још нема фотографија.</p>"}</div><div class="photo-drop" data-photo-drop><div><strong>Изаберите фотографије са рачунара или телефона, или их превуците овдје.</strong><p class="help">JPEG, PNG или WebP · до 10 фотографија · највише 20 MB по оригиналу</p><label class="button secondary" for="photo-input">Изабери фотографије</label><input class="photo-input" id="photo-input" data-photo-input type="file" accept="image/*" multiple></div></div><div class="photo-local-list" data-photo-local-list></div><div class="photo-actions"><button class="button" type="button" data-upload-photos disabled>Отпреми фотографије</button><span data-photo-status role="status"></span></div><p class="help photo-rights">Отпремањем потврђујете да је фотографија ауторски материјал пројекта и да је дозвољена за веб-приказ.</p></section>
  <section class="panel editor-section" id="tekst"><h2>Српски текст</h2><p class="help">Канонски ID и редослед одјељака остају сачувани.</p><div data-section-list>${p.sections.map(sectionHtml).join("")}</div><div class="actions"><select data-new-section>${available.map((id) => `<option value="${id}">${id}</option>`).join("")}</select><button class="button secondary" type="button" data-add-section>Додај канонски одјељак</button></div></section>
  <div class="validation-summary panel error" data-validation-summary role="alert"></div><div class="save-bar"><span class="is-clean" data-dirty-status>Нема несачуваних измјена.</span><div class="actions"><span data-save-status role="status"></span><button class="button" data-save>Сачувај</button></div></div></form>
  ${dangerZone}<template data-alternate-template><div class="alternate-row" data-alternate-row><div class="editor-grid"><label class="field">Назив<input data-alt-name></label><label class="field">Статус<select data-alt-status>${optionList(record.options.verificationStatuses, "requires-verification")}</select></label><label class="field wide">Контекст<textarea data-alt-context></textarea></label></div><button type="button" class="button secondary" data-remove-alternate>Уклони назив</button></div></template>`;
  return layout(`Уреди: ${p.preferredName}`, session, body, "", true);
}

const routePlaceOptions = (places: RouteRepositorySnapshot["places"], selected: string) => places.map((place) => `<option value="${escapeHtml(place.id)}"${place.id === selected ? " selected" : ""}>${escapeHtml(place.name)} · ${escapeHtml(place.id)}</option>`).join("");
const routeMetric = (route: AdminRoute, key: string, suffix = "") => typeof route.metrics[key] === "number" ? `${escapeHtml(route.metrics[key])}${suffix}` : "—";

export function routesPage(session: AdminSession, snapshot: RouteRepositorySnapshot): Response {
  const placeName = new Map(snapshot.places.map((place) => [place.id, place.name]));
  const rows = snapshot.routes.map((route) => `<tr><td><strong>${escapeHtml(route.name)}</strong><br><span class="help">${escapeHtml(route.id)}</span></td><td>${escapeHtml(placeName.get(route.startPlaceId) ?? route.startPlaceId)}</td><td>${escapeHtml(placeName.get(route.endPlaceId) ?? route.endPlaceId)}</td><td>${routeMetric(route, "distance_m", " m")}</td><td>${escapeHtml(route.difficulty)}</td><td>${route.trackStatus === "ready" ? `${escapeHtml(route.pointCount)} тачака` : "Нема"}</td><td><span class="badge ${route.inPreview ? "preview-on" : "preview-off"}">${route.inPreview ? "Да" : "Не"}</span></td><td><a href="/routes/${encodeURIComponent(route.id)}/edit">Уреди</a></td></tr>`).join("");
  return layout("Руте", session, `<div class="toolbar"><div><p class="eyebrow">Git садржај</p><h1>Руте</h1></div><a class="button" href="/routes/new">Нова рута</a></div><div class="panel table-wrap"><table class="table"><thead><tr><th>Назив</th><th>Полазиште</th><th>Одредиште</th><th>Дужина</th><th>Тежина</th><th>GPS</th><th>Радни приказ</th><th>Акција</th></tr></thead><tbody>${rows || '<tr><td colspan="8">Још нема рута.</td></tr>'}</tbody></table></div>`);
}

export function newRoutePage(session: AdminSession, snapshot: RouteRepositorySnapshot): Response {
  const options = routePlaceOptions(snapshot.places, "");
  return layout("Нова рута", session, `<p class="eyebrow">Research scaffold</p><h1>Нова рута</h1><p class="help">Креирање не додаје руту у радни приказ. GPX се учитава након чувања основног записа.</p><form class="panel form" data-route-create><label class="field">Назив<input name="preferredName" required></label><label class="field">Краћи назив<input name="shortName"></label><label class="field">Технички ID<input name="id" pattern="[a-z0-9]+(?:-[a-z0-9]+)*" required></label><label class="field">Slug<input name="slug" pattern="[a-z0-9]+(?:-[a-z0-9]+)*" required></label><label class="field">Полазна светиња<select name="startPlaceId" required><option value="">— Изаберите —</option>${options}</select></label><label class="field">Крајња светиња<select name="endPlaceId" required><option value="">— Изаберите —</option>${options}</select></label><input type="hidden" name="expectedHeadSha" value="${escapeHtml(snapshot.state.headSha)}"><button class="button">Сачувај research руту</button><p data-status role="status"></p></form>`, `const f=document.querySelector('[data-route-create]'),s=f.querySelector('[data-status]');f.addEventListener('submit',async e=>{e.preventDefault();const r=await fetch('/api/routes',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(new FormData(f)))}),d=await r.json();if(!r.ok){s.textContent=d.error?.message??'Грешка';return}location.href='/routes/'+encodeURIComponent(d.routeId)+'/edit'});`);
}

export function editRoutePage(session: AdminSession, snapshot: RouteRepositorySnapshot, route: AdminRoute, mapTilerKey?: string): Response {
  const sectionIds = ["about-route", "route-course", "water-rest", "safety", "equipment", "notes"];
  const sectionLabels: Record<string,string> = { "about-route": "О рути", "route-course": "Ток руте", "water-rest": "Вода и одмор", safety: "Безбједност", equipment: "Опрема", notes: "Напомене" };
  const sections = sectionIds.map((id) => { const section = route.sections.find((candidate) => candidate.id === id); return `<label class="field"><span>${sectionLabels[id]}</span><textarea data-route-section="${id}" data-route-section-title="${sectionLabels[id]}">${escapeHtml(section?.paragraphs.join("\n\n") ?? "")}</textarea></label>`; }).join("");
  const trackJson = route.track ? JSON.stringify(route.track).replaceAll("<", "\\u003c") : "null";
  const checked = (values: string[], value: string) => values.includes(value) ? " checked" : "";
  const selected = (actual: string, expected: string) => actual === expected ? " selected" : "";
  const body = `<div class="toolbar"><div><p class="eyebrow">Уређивање руте</p><h1>${escapeHtml(route.name)}</h1><p class="help">ID <code>${escapeHtml(route.id)}</code> је непромјенљив · ${escapeHtml(route.editorialStatus)}</p></div><a class="button secondary" href="/routes">Све руте</a></div>
  <section class="panel preview-control" data-route-preview data-enabled="${route.inPreview}"><div><h2>Радни приказ</h2><span class="badge ${route.inPreview ? "preview-on" : "preview-off"}" data-preview-badge>${route.inPreview ? "У радном приказу" : "Није у радном приказу"}</span><p data-preview-status role="status"></p></div><button class="button secondary" type="button" data-preview-toggle>${route.inPreview ? "Уклони из радног приказа" : "Додај у радни приказ"}</button></section>
  <nav class="editor-nav"><a href="#route-basic">Основно</a><a href="#route-track">Траса</a><a href="#route-practical">Практично</a><a href="#route-text">Српски текст</a></nav>
  <form class="editor" data-route-editor data-route-id="${escapeHtml(route.id)}" data-head-sha="${escapeHtml(snapshot.state.headSha)}" data-map-key="${escapeHtml(mapTilerKey ?? "")}">
    <section class="panel editor-section" id="route-basic"><h2>Основно</h2><div class="editor-grid"><label class="field">Назив<input name="preferredName" value="${escapeHtml(route.name)}" required></label><label class="field">Краћи назив<input name="shortName" value="${escapeHtml(route.shortName)}"></label><label class="field">Slug<input name="slug" value="${escapeHtml(route.slug)}" required></label><label class="field">Тип<select name="routeType"><option value="hiking">Пјешачка</option></select></label><label class="field">Смјер<select name="direction"><option value="one-way"${route.direction === "one-way" ? " selected" : ""}>У једном смјеру</option><option value="out-and-back"${route.direction === "out-and-back" ? " selected" : ""}>Тамо и назад</option><option value="circular"${route.direction === "circular" ? " selected" : ""}>Кружна</option></select></label><label class="field">Полазна светиња<select name="startPlaceId">${routePlaceOptions(snapshot.places, route.startPlaceId)}</select></label><label class="field">Крајња светиња<select name="endPlaceId">${routePlaceOptions(snapshot.places, route.endPlaceId)}</select></label><label class="field wide">Сажетак<textarea name="summary">${escapeHtml(route.summary)}</textarea></label></div></section>
    <section class="panel editor-section" id="route-track"><h2>Траса</h2><div class="map-picker" data-route-map><div class="map-picker-note">${mapTilerKey && route.track ? "Учитавање Garmin трасе…" : route.track ? "Карта није доступна без PUBLIC_MAPTILER_KEY." : "Учитајте GPX да бисте приказали трасу."}</div></div><script type="application/json" data-route-track>${trackJson}</script><dl class="grid facts"><div><dt>Дужина</dt><dd>${routeMetric(route,"distance_m"," m")}</dd></div><div><dt>Успон</dt><dd>${routeMetric(route,"ascent_m"," m")}</dd></div><div><dt>Спуст</dt><dd>${routeMetric(route,"descent_m"," m")}</dd></div><div><dt>Најнижа / највиша</dt><dd>${routeMetric(route,"min_elevation_m"," m")} / ${routeMetric(route,"max_elevation_m"," m")}</dd></div><div><dt>Снимљено вријеме</dt><dd>${routeMetric(route,"recorded_duration_minutes"," min")}</dd></div><div><dt>Број тачака</dt><dd>${escapeHtml(route.pointCount ?? "—")}</dd></div></dl><div class="photo-actions"><label class="button secondary" for="route-gpx">${route.track ? "Замијени GPX" : "Учитај GPX"}</label><input class="photo-input" id="route-gpx" type="file" accept=".gpx,application/gpx+xml" data-route-gpx><button class="button secondary" type="button" data-upload-gpx disabled>Учитај GPX</button>${route.track ? '<button class="button danger" type="button" data-remove-track>Уклони трасу</button>' : ""}<span data-gpx-status role="status"></span></div></section>
    <section class="panel editor-section" id="route-practical"><h2>Практично</h2><div class="editor-grid">
      <label class="field">Процијењено вријеме (минути)<input type="number" min="1" name="estimatedDurationMinutes" value="${escapeHtml(route.metrics.estimated_duration_minutes ?? "")}"></label>
      <label class="field">Тежина<select name="difficulty"><option value="easy"${selected(route.difficulty,"easy")}>Лака</option><option value="moderate"${selected(route.difficulty,"moderate")}>Средње захтјевна</option><option value="demanding"${selected(route.difficulty,"demanding")}>Захтјевна</option></select></label>
      <label class="field wide">Приступ полазишту<textarea name="startAccessNote">${escapeHtml(route.practical.startAccessNote ?? "")}</textarea></label>
      <label class="field">Паркинг<select name="parkingStatus"><option value="unknown"${selected(route.practical.parkingStatus,"unknown")}>Није провјерено</option><option value="available"${selected(route.practical.parkingStatus,"available")}>Доступан</option><option value="limited"${selected(route.practical.parkingStatus,"limited")}>Ограничен</option><option value="none"${selected(route.practical.parkingStatus,"none")}>Нема паркинга</option></select></label>
      <label class="field">Напомена о паркингу<textarea name="parkingNote">${escapeHtml(route.practical.parkingNote ?? "")}</textarea></label>
      <label class="field">Маркација стазе<select name="trailMarkingStatus"><option value="unknown"${selected(route.practical.trailMarkingStatus,"unknown")}>Није провјерено</option><option value="marked"${selected(route.practical.trailMarkingStatus,"marked")}>Маркирана</option><option value="partially-marked"${selected(route.practical.trailMarkingStatus,"partially-marked")}>Дјелимично маркирана</option><option value="unmarked"${selected(route.practical.trailMarkingStatus,"unmarked")}>Није маркирана</option></select></label>
      <label class="field">Напомена о маркацији<textarea name="trailMarkingNote">${escapeHtml(route.practical.trailMarkingNote ?? "")}</textarea></label>
      <label class="field">Захтјевни дјелови<select name="difficultSectionsStatus"><option value="unknown"${selected(route.practical.difficultSectionsStatus,"unknown")}>Није провјерено</option><option value="none"${selected(route.practical.difficultSectionsStatus,"none")}>Нема посебно захтјевних дјелова</option><option value="present"${selected(route.practical.difficultSectionsStatus,"present")}>Постоје захтјевни дјелови</option></select></label>
      <label class="field">Напомена о захтјевним дјеловима<textarea name="difficultSectionsNote">${escapeHtml(route.practical.difficultSectionsNote ?? "")}</textarea></label>
      <label class="field wide">Препоручена обућа<input name="footwearRecommendation" value="${escapeHtml(route.practical.footwearRecommendation ?? "")}"></label>
      <label class="field">Мобилни сигнал<select name="mobileSignalStatus"><option value="unknown"${selected(route.practical.mobileSignalStatus,"unknown")}>Није провјерено</option><option value="good"${selected(route.practical.mobileSignalStatus,"good")}>Добар</option><option value="variable"${selected(route.practical.mobileSignalStatus,"variable")}>Промјенљив</option><option value="poor"${selected(route.practical.mobileSignalStatus,"poor")}>Слаб</option><option value="none"${selected(route.practical.mobileSignalStatus,"none")}>Нема сигнала</option></select></label>
      <label class="field">Напомена о сигналу<textarea name="mobileSignalNote">${escapeHtml(route.practical.mobileSignalNote ?? "")}</textarea></label>
      <label class="field wide">Вријеме / вјетар / изложеност<textarea name="weatherNote">${escapeHtml(route.practical.weatherNote ?? "")}</textarea></label>
      <label class="field">Последња провјера руте<input type="date" name="lastVerifiedAt" value="${escapeHtml(route.practical.lastVerifiedAt ?? "")}"></label>
      <label class="field">Вода<select name="waterStatus"><option value="unknown"${selected(route.waterStatus,"unknown")}>Није провјерено</option><option value="none"${selected(route.waterStatus,"none")}>Нема</option><option value="available"${selected(route.waterStatus,"available")}>Доступна</option><option value="requires-verification"${selected(route.waterStatus,"requires-verification")}>Захтијева провјеру</option></select></label>
      <label class="field wide">Напомена о води<textarea name="waterNote">${escapeHtml(route.waterNote ?? "")}</textarea></label>
    </div><fieldset><legend>Подлога</legend>${["paved","gravel","trail","rock","steps","other"].map((value) => `<label><input type="checkbox" name="surface" value="${value}"${checked(route.surface,value)}> ${value}</label>`).join(" ")}</fieldset><fieldset><legend>Препоручени период</legend>${["spring","summer","autumn","winter"].map((value) => `<label><input type="checkbox" name="recommendedSeasons" value="${value}"${checked(route.recommendedSeasons,value)}> ${value}</label>`).join(" ")}</fieldset><label><input type="checkbox" name="featured"${route.featured ? " checked" : ""}> Прикажи у Популарним рутама</label><label class="field">Featured order<input type="number" min="1" name="featuredOrder" value="${route.featuredOrder}"></label></section>
    <section class="panel editor-section" id="route-text"><h2>Српски текст</h2>${sections}</section><div class="validation-summary panel error" data-validation-summary role="alert"></div><div class="save-bar"><span data-dirty-status>Нема несачуваних измјена.</span><div class="actions"><span data-save-status role="status"></span><button class="button" data-save-route>Сачувај</button></div></div>
  </form><section class="panel danger-zone"><p class="eyebrow">Опасна зона</p><h2>Обриши руту</h2><p>Бришу се запис, српски текст, траса и чланство у радном приказу. Повезане светиње и њихове фотографије остају нетакнуте.</p><label class="field">За потврду унесите ID руте<input data-delete-route-confirmation></label><button class="button danger" type="button" data-delete-route disabled>Трајно обриши руту</button><p data-delete-status role="status"></p></section>`;
  return layout(`Уреди: ${route.name}`, session, body, "", "route");
}
